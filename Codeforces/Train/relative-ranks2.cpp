#include <bits/stdc++.h>
using namespace std;

int main(){
  const long long num = 1e6+5;
  // memset(arr, 0, sizeof(arr));
  vector<int> a = {10, 3, 8, 9, 4};
  cout << a[1] << " " << a[2];
  long long arr[num];
  // for(int i = 0; i <= 10; i++){
  //   cout << arr[i];
  // }
}


// vector<string> findRelativeRanks(vector<int>& score) {
        
// }

